#include "Polynom.cpp"
#include "Grid.cpp"

class PolynomLag : public Polynom {
    public: 
        PolynomLag(Grid grid){
                double divider =0;
                
                for(int i = 0; i < grid.size() ; ++i){
                    for(int j = 0; j < grid.size() - 1; ++j){
                        
                    }   
                }
        }
};